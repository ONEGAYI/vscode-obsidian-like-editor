# Changelog

本文件记录面向用户的版本变更。格式遵循 Keep a Changelog 约定：每个版本一个
`## <版本号> - <日期>` 段落，条目按「新增 / 改进 / 修复」归类。发布脚本
（`scripts/release.mjs`）会提取最新版本段落作为 GitHub Release 的发布说明，
并校验最新版本号与 `package.json` 的 `version` 一致——新增条目请追加在最新
段落内，发新版时新建段落并同步升版本号。

## 0.2.0 - 2026-09-26

本版本围绕信息组织与内容呈现扩展：新增右侧栏大纲面板、公式渲染、
Mermaid 图表与代码块卡片，并增强表格选区交互。

### 新增

- **右侧栏与大纲面板**：编辑器顶栏新增右侧栏切换按钮（细线／粗线两态
  图标，状态跨面板记忆）。「大纲」面板实时同步标题层级（ATX/Setext
  语义，代码围栏与 frontmatter 伪标题不计入）；点击条目跳转正文对应
  标题（双模式），常驻高亮指示当前所在章节；六档「结绳」滑块统一展开
  深度，条目箭头手动折叠，滚动自动展开当前路径；工具条提供跳转末尾、
  重置与标题搜索（片段级高亮）；标题行内样式（粗体、斜体、行内代码、
  删除线）与主题层级配色同源透传；右键菜单提供结构命令、五项复制、
  调整层级、重命名（保留行内标记）与整段删除；拖拽标题按「控制域」
  整段重排，控制域外内容零误伤，一次撤销完整回滚。（PR #64、#71）
- **公式渲染**：行内 `$…$` 与块级 `$$…$$` 的 LaTeX 公式在实时预览与
  阅读模式渲染（KaTeX 本地打包，无 CDN）；光标进入公式显形源码直接
  编辑，解析失败稳定降级为可读原文；普通美元、转义与代码区不误判。
  （PR #76）
- **Mermaid 图表**：语言标记为 `mermaid` 的围栏代码块在实时预览与阅读
  模式渲染为图（流程图、时序图等；本地打包按需加载，无 CDN）；光标
  进入围栏显形源码直接编辑，语法错误降级为错误提示与可读源码，不影响
  后续内容；明暗主题自动联动，图内链接不执行跳转。（PR #76）
- **代码块卡片**：围栏代码块在光标离开时收起为卡片——语言头部横带
  （彩色徽标 + 显示名）、卡内行号（每块从 1 起）、悬停复制按钮（点击复制
  整块代码，经宿主剪贴板按文档换行风格写入——CRLF 文档得 CRLF；编辑态
  同样常驻）、折叠 chevron（点击收起，光标进入临时展开）；内置 Lezer +
  legacy-modes 高亮引擎（17 语言 + 纯文本，支持别名，明暗两套 VSCode
  Dark+/Light+ 风格色板，未识别语言回退纯文本），实时预览与阅读视图观感
  一致。mermaid 等渲染型围栏在光标进入（含点击渲染图）时同样显示卡片
  外壳与行号——呈现态保持图表渲染，折叠收起同样支持。设置页提供
  「代码块卡片 / 卡内行号 / 复制按钮 / 语法高亮」四个开关（高亮独立于
  卡片）。（PR #87）

### 改进

- **表格选区**：跨格拖选形成矩形高亮，Ctrl+C / Cmd+C 复制为独立
  Markdown 表格（选区首行为表头）；Delete / Backspace 清空局部区域，
  满行／满列选区删除对应行／列，整表选区删除整表，空行首格起点退格
  删除该行；边沿点阵把手支持单击选中行列（原有拖动重排保留）。
  （PR #76）

### 已知限制

- 代码块卡片：超过 4096 行的围栏跳过语法着色（逐键全块解析不可接受，
  回退纯文本卡片）；语言徽标为字形徽标（非矢量 logo）。

## 0.1.0 - 2026-09-25

首个公开版本。

### 新增

- **双视图编辑器**：基于源文本的「实时预览 + 阅读」双视图；实时预览由
  CodeMirror 6 全文承载（视口外不建 DOM），阅读模式由 markdown-it 分块渲染
  并按需挂载，10 万行 / 10 万块级文档性能与体量基本无关。
- **三态切换**：编辑器标题栏按钮在「实时预览 → 阅读 → 源码编辑器」间循环，
  命令面板亦可直达任一模式；最近停留模式跨窗口全局记住，源码位置锚点保持。
  `.md` / `.markdown` 默认由本扩展打开，可随时右键改回原生编辑器。
- **表格编辑**：规范表格以网格呈现，点击单元格直接格内编辑且网格保持；
  悬停入口增添行列、点阵抓手拖排行；Tab / Shift+Tab 单元格导航；命令面板
  六个行列命令与「创建表格」命令（中英双语界面）；格内键入 `|` 自动转义。
- **链接、图片与双链**：普通链接、图片与 `[[双链]]` 在光标进入时显形源码，
  单击跳转（源码态 Ctrl/Cmd + 单击）；双链支持 `[[笔记]]`、`[[路径/笔记]]`、
  `[[笔记|别名]]`、`[[笔记#标题]]` 四形态，重名弹候选、缺失目标提示；
  本地图片经宿主通道装载，`file://`、`javascript:` 等危险 scheme 拦截。
- **任务勾选**：两种视图内点击 checkbox 直接写回源文本，支持撤销。
- **查找**：编辑器内 Ctrl+F / Cmd+F（限本编辑器激活时）。
- **独立设置页**：「Vsidian: 打开设置」进入扩展自带设置页，不占用 VSCode
  统一设置中心；设置用户级保存、重开回显、变更即时生效。当前提供实时预览
  行号开关（源文件行号，表格段显示段首行号）。
- **输入与同步防护**：中文等 IME 组合期间缓冲上报，避免半截候选写回；
  外部修改无法安全同步时弹出冲突横幅，本地输入不丢（可复制或放弃）。

### 已知限制

- 未支持 Obsidian 的Canvas、白板、插件生态与 `.md` 之外的笔记格式。
- CSP 允许 `img-src https:`——任意 https 图源可达（远程图床支持的设计代价）。
- 阅读模式始终无行号；表格列数不一致时显示可编辑源码而非网格。

<!-- 变更链接
0.1.0: https://github.com/ONEGAYI/vsidian/commits/v0.1.0
0.2.0: https://github.com/ONEGAYI/vsidian/compare/v0.1.0...v0.2.0
-->
