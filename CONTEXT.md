# Markdown 编辑体验

本项目是在 VSCode 中提供类 Obsidian Markdown 编辑体验的扩展。当前目标是形成可用的最小产品，支持实时预览、阅读模式与原生源码编辑器之间的三态切换（#38 起 .md 默认由本扩展打开）。

## 领域语言

**实时预览（Live Preview）**：允许直接编辑 Markdown 的视图；内容呈现格式效果，进入相关内容编辑时显露对应语法。
避免称为“全程隐藏语法的富文本编辑器”，以免混淆已明确的产品方向。

**阅读模式**：以适合阅读的格式呈现 Markdown 的视图，与实时预览可切换。允许勾选或取消任务状态，其余内容只读。
避免单独使用“预览”指代此模式，以免与实时预览混淆。

**双链（wikilink）**：采用 Obsidian 链接语法、可显示并跳转到目标的链接。首版支持笔记名、目录路径、显示文字与标题锚点，不包括反向链接索引和 Markdown 嵌入。

**表格编辑**：在实时预览中直接编辑普通 Markdown 表格的单元格，支持 Tab 切换及增删行列。后续交互以已确认的 [表格交互规格](docs/specs/table-interaction-rework.md)（#72）为准：跨格矩形选择、独立 Markdown 复制、按覆盖范围删行列、边沿悬停新增及行列插入式拖排。不包含合并单元格、列宽拖拽调整、任意单格搬移与 Excel 式区域粘贴。

**单元格区域选区**：以起止单元格确定的矩形范围；高亮、复制与删除共用此范围。格内局部文字仍使用普通文本选区。复制区域以选区首行为表头；删除局部区域清内容，覆盖完整行或列则删除对应结构，同时覆盖全表则删除整表。

**表格把手**：紧贴左边沿或上边沿、悬停显示的点阵控件；单击选择对应整行或整列，拖动按插入位置移动。表头参与移动，移动后的首行成为新表头，分隔行跟随新表头。

**源码降级**：不支持的复杂内容局部显示为 Markdown 源码并允许编辑，同时保留原文。此方式可用于首版尚未支持的内容，但不能替代表格与双链已经确认的基本功能要求。

**行内围栏**：成对包裹行内文字的语法标记，现行四种为粗体、斜体、删除线、行内代码。切换语义指套用与取消互为逆操作：有选区作用于选区；无选区时取光标所在或其右侧的词（行末取左侧词），取不到词时（光标在空白或标点上）插入空围栏对、光标居其中；包裹后光标位于标记内侧，再次切换即取消。普通链接同属切换语义（取消去除链接结构、保留文字）；行内公式与双链属插入结构——无选区时插入空结构等待输入，不具备切换语义，需要两态化属新需求。块级围栏（代码块与块级公式的整体包裹结构）是另一概念，其切换按段落整体包裹或取消，不与行内围栏混称。

## 已确认的产品边界

- 产品形态是 VSCode Markdown 编辑器扩展。
- 实时预览与阅读模式均属于目标体验，用户可以切换。
- 本轮通过设计访谈确定可用最小产品的范围。
- 首版必须包含可直接编辑单元格的表格与 Obsidian 语法的基础双链。
- 双链支持 `[[笔记]]`、`[[目录/笔记]]`、`[[笔记|显示文字]]`、`[[笔记#标题]]` 的显示与跳转。
- 双链目标限定在当前文件所属工作区文件夹内，按需查找，不建立持久索引；多个同名候选由用户选择，不存在时提示，首版不自动创建文件。
- 首版需要支持 Windows 本地使用及 Remote SSH，最低兼容 VSCode 1.86。用户远程环境为另一台 Windows 的 VSCode 1.86 连接 CentOS 7；主要兼容验收环境为本机 Windows 便携版 VSCode 1.86。
- 允许不支持的复杂内容局部退回源码；必须保留原文。中文输入、保存、撤销与模式切换不得丢失内容。

## 首版操作与交付

- 基础语法包包含标题、粗斜体、列表、任务列表、引用、代码块、普通链接及本地图片；另包含上述表格与双链能力。
- 提供编辑区内查找。
- 阅读模式单击链接跳转；实时预览单击进入编辑，`Ctrl+单击` 跳转链接。
- 两种模式均允许勾选或取消任务状态。阅读模式的任务操作修改同一 Markdown 文档，适用相同保存、撤销及冲突规则，不是仅改变页面外观。
- 外部变更能安全同步时自动更新。无法安全同步时保留未确认输入、暂停写回并提示恢复，不能静默覆盖。
- 首版支持外部变更同步，不承诺多个编辑视图同时输入的协同编辑体验。
- 交付可安装的 VSIX。#38 起注册为 .md 的默认编辑器：打开 Markdown 文件默认进入本扩展，用户可经「Reopen With…」或编辑器关联设置改回原生。
- 标题栏三态按钮在实时预览、阅读模式、源码编辑器之间循环切换（实时预览 → 阅读 → 源码 → 实时预览）；命令面板保留同名入口，最近一次停留的模式跨窗口全局记住。
- 一期前端为二期自定义 CSS 片段预留稳定的样式入口；建立编辑内容区域与 Obsidian 同款选择器的映射及兼容验证，避免将可公开样式入口全部变为随机类名或不可访问的内部节点。片段加载功能属于二期。
- MVP 必须采用全文文本模型与按需 DOM 渲染分离的设计。编辑视图保留 CM6 视口虚拟渲染，自定义装饰、图片与表格不得退回整篇 DOM 常驻；这不是文件流式读取。
- 阅读视图单独采用分块按需挂载设计，不能把 CM6 的视口能力当作 markdown-it 已提供的能力。保留定位、任务状态与样式入口，测量长文档的节点增长及回收。

## 已接受的技术路线

使用 `CustomTextEditorProvider + Webview + CodeMirror 6` 实现实时预览，以 markdown-it 实现阅读渲染。两种视图共用 VSCode `TextDocument`，由宿主管理权威文本与文件生命周期。具体同步协议和依赖版本在规格及原型验证中落实。

## 二期功能记录

以下功能按用户要求记录为二期，不在本轮悄然删去，也不代表已经确定二期实现顺序。

- callout 提示块。
- 图片粘贴。
- 双链扩展能力：索引与 Markdown 嵌入等。
- 块引用 `#^id` 与重命名后自动更新引用。
- 自定义 CSS 片段与 Obsidian 同款选择器；一期预留前端样式契约，二期实现加载与兼容验证。具体支持清单需逐项记录，不将整个 Obsidian 应用的 DOM 兼容性当作已实现事实。

表格与基础双链属于首版，是此前“额外候选”列表中的明确例外。公式渲染与编辑体验已由 #59 交付（`$…$` / `$$…$$` 双视图渲染与实时预览编辑）；Mermaid 围栏图表已由 #60 交付（`mermaid` 围栏双视图渲染、无效语法降级、明暗主题联动，本地打包按需加载）——两者自边界清单移出。

## 三期考察

考察“插件上插件”：本扩展是否提供可加载其他扩展模块的能力。该项是可行性研究，不是已经承诺实现插件 SDK，也不等于承诺直接运行 Obsidian 社区插件。

## 决策状态

用户已确认完整 MVP 基线；工单 #2–#15 的功能实现和自动化回归已完成，#1 的人工验收仍按[人工验证清单](docs/specs/manual-verification.md)逐项记录。验收复核问题见工单 #21–#27。[调研报告](docs/research/obsidian-live-preview-editor.md)保留历史调研内容；范围和模式行为以本文件及已接受的 ADR 为准。

已确认的兼容性约束见 [ADR-0001](docs/adr/0001-vscode-186-remote-support.md)。
首版双链解析边界见 [ADR-0002](docs/adr/0002-wikilink-on-demand-resolution.md)。
编辑架构与数据一致性见 [ADR-0003](docs/adr/0003-source-text-dual-view-editor.md)。
样式兼容预留见 [ADR-0004](docs/adr/0004-stable-styling-contract.md)。
按需渲染约束见 [ADR-0005](docs/adr/0005-viewport-rendering.md)。
